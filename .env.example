# SQL Prep Lab is fully local-first: it needs no environment variables to build or run.
#
# If you later wire up optional integrations (telemetry, etc.), declare them here and
# reference them with NEXT_PUBLIC_ prefixes where they must be readable in the browser.
# No Vercel Functions or secrets are used by the MVP.
